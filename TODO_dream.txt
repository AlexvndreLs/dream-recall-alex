# TODO — Projet DREAM (preprocessing → feat_extract → classify)
# Mis à jour : 22 juin 2026
# État : batch 38 sujets terminé, preprocessing complet, seuil de rejet ICA à trancher

================================================================================
BLOC 1 — BLOQUANT (à faire avant feat_extract)
================================================================================

[ ] 1. Lancer analyze_thresholds.py — trancher la méthode de rejet ICA
        Pourquoi bloquant : le rejet actuel (z-score 2.5) est erratique entre
        sujets (0 rejet sur s26/s28, 10 rejets sur s38). Les branches
        preprocessed-ica ne sont pas fiables tant que le seuil n'est pas fixé.

        Ce que fait le script :
        - recharge les 38 ICA sauvées (sans re-fitter)
        - recalcule par composante : corrélation EOG absolue, z-score EOG, score muscle
        - dump thresholds_scores.csv + trace thresholds_curves.png
          (nb rejets moyen/sujet ± écart-type + % sujets à 0, vs seuil continu)

        Commande :
            salloc --account=def-kjerbi --time=1:30:00 --cpus-per-task=4 --mem=64G
            cd ~/dream-recall-alex && source /home/alouis/mne_env/bin/activate
            python analyze_thresholds.py \
                --bids-path  /home/alouis/scratch/dream_bids \
                --deriv-root /home/alouis/scratch/dream_bids/derivatives \
                --out-dir    ./threshold_analysis

        Critère de décision sur thresholds_curves.png :
        - méthode dont la courbe est la plus PLATE (faible variance inter-sujets)
        - aucun sujet à 0 rejet sur une plage de seuils raisonnable
        - hypothèse à vérifier : corrélation absolue (~0.5) > z-score (erratique)

        Suite si corrélation absolue gagne :
        - modifier run_ica : measure='zscore', threshold=2.5
                          → measure='correlation', threshold=<valeur choisie>
        - ré-appliquer le rejet sur les 38 sujets SANS re-fit :
          recharger l'ICA .fif, rejouer find_bads_eog avec le nouveau seuil,
          ica.apply, réécrire le BIDS preprocessed-ica/ (script léger à écrire,
          pas besoin de relancer tout le preprocessing)

        Nuance connue : corrélation 0.5 peut attraper ICA001 de s4 (onde lente
        delta qui déborde sur EOG). La branche noica sert de garde-fou empirique
        (si le rejet détruit du signal, noica sera meilleure en classification).

[ ] 2. Vérifier/régénérer le BIDS du sujet 4 (hypnogramme décalé)
        Problème : s4 a un offset de 1 sample dans hyp_per_s4.txt (29 samples
        au lieu de 30 pour la première epoch). Le fix est dans mat_eeg_to_bids :
        HYPNO_FIXES = {(4, 'per'): 1} (prepend 1 sample = hypno[0]).

        À vérifier :
        - est-ce que le BIDS de s4 a été régénéré avec ce fix AVANT le batch ?
        - si non : régénérer le BIDS de s4 (mat_eeg_to_bids_v2.py 4 ...)
          puis re-preprocesser s4 (les 3 branches, sbatch --array=4 ...)

        Si le BIDS n'est pas corrigé, les epochs de s4 seront décalées d'1 sample
        → les stades seront mal assignés → les features de s4 seront fausses.
        Pas bloquant pour les 37 autres sujets.

================================================================================
BLOC 2 — PATCHES DE FOND (pendant le feat_extract, avant d'interpréter)
================================================================================

[ ] 3. Max-statistics dans classify_vector (#6)
        Où : classify.py, fonction classify_vector (ou équivalent)
        Quoi : ajouter pvals_maxstat depuis perm_accs.max(axis=1) comme
               distribution nulle partagée (correction pour comparaisons multiples).
               Garder pvals non corrigé en parallèle pour diagnostic.
        Taille : ~10 lignes. Ne change pas la logique de classification,
                 seulement la correction des p-values.

[ ] 4. Vérifier forme et SPD-itude de CoSpectra (#8)
        Où : feat_extract_umap_fooof_v4.py
        Quoi : print(CoSpectra(...).fit_transform(data[:2]).shape) pour
               confirmer l'axe de moyenne fréquentielle.
               Vérifier que les matrices sont bien SPD (definies positives
               symétriques) — sinon TSclassifier Riemannien plante en silence
               ou avec une erreur cryptique.
        Commande de vérification rapide (sur 2 sujets de test) :
            from pyriemann.estimation import CoSpectra
            import numpy as np
            X = np.random.randn(2, 19, 7500)  # 2 epochs, 19 canaux, 7500 samples
            csp = CoSpectra().fit_transform(X)
            print(csp.shape)  # attendu : (2, 19, 19, n_freqs) ou (2, n_freqs, 19, 19)
            # vérifier SPD : toutes valeurs propres > 0
            from numpy.linalg import eigvalsh
            print((eigvalsh(csp[0, 0]) > 0).all())

[ ] 5. Trancher combine_classification_state (#9)
        Où : feat_extract_umap_fooof_v4.py
        Problème : code mort — classify.py recharge les fichiers atomiques
                   (S1/S2/S3/S4/REM séparément), ne lit jamais les .npz combinés
                   produits par combine_classification_state.
        Décision à prendre : soit supprimer la fonction (gagne de l'I/O),
                             soit faire lire le combiné par classify.
        Recommandation : supprimer, c'est du code inutile qui ralentit
                         feat_extract sans bénéfice.

[ ] 6. Vérifier StratifiedLeave2GroupsOut (#10)
        Où : classify.py
        Quoi : confirmer que get_n_splits() = n_HR × n_LR
               (1 HR + 1 LR laissés en test à chaque split, tous les autres
               en train — conforme à la thèse Arthur §1.2.7).
        Commande :
            from sklearn.model_selection import LeavePGroupsOut
            cv = StratifiedLeave2GroupsOut()
            print(cv.get_n_splits(X, y, groups))  # attendu : 18 × 18 = 324 ou équivalent

================================================================================
BLOC 3 — APRÈS QUE LE PIPELINE TOURNE ENTIÈREMENT
================================================================================

[ ] 7. compare_fooof_mode.py → décision knee vs fixed → psd_peak_*
        Script déjà créé et prêt à lancer.
        Lit les données preprocessed-ica/, fitte specparam en mode fixed et knee,
        compare delta_R2 (>0.02 → knee nécessaire) et delta_npeaks (>0.8 → fixed
        surajoute des pics fantômes).
        Après décision : ajouter feature psd_peak_* (somme des hauteurs de pics
        gaussiens par bande, 0 si aucun pic) dans feat_extract.

[ ] 8. Comparaison ica / noica / iclabel en classification
        C'est le garde-fou empirique principal : lancer classify.py sur les 3 branches
        et comparer les accuracies.
        - ica ≈ noica → le rejet ICA n'aide ni ne nuit (neutre)
        - ica > noica → le rejet ICA améliore (artefacts retirés = bruit en moins)
        - noica > ica → le rejet ICA détruit du signal utile (→ revoir les seuils)
        - iclabel ~ noica → confirme qu'ICLabel ne rejette pas grand chose (déjà vu)
        Cette comparaison justifie (ou invalide) empiriquement la méthode de rejet
        choisie en point 1.

[ ] 9. Validation méthodo
        a) Complexité vs aperiodic seul (#11)
           perm_entropy, higuchi_fd, spec_entropy : vérifier qu'elles ajoutent de
           l'info au-delà de la pente 1/f (corrélation + accuracy marginale).
           spec_entropy en particulier (dérivée du même spectre Welch) est suspecte
           de redondance. LZC déjà exclu (trop corrélé au slope).

        b) n_trials_min ≈ 61 (#12)
           Confirmer que compute_global_n_trials retombe dans l'ordre de grandeur
           d'Arthur. Un écart fort signale un problème d'épochage ou d'exclusion.

        c) Alignement hypnogramme s4 dans le BIDS (#13)
           (lié au point 2 ci-dessus — à traiter dès que possible)

================================================================================
PARKED (gardé en tête, pas prioritaire)
================================================================================

[ ] overlap cosp à 0.01 — laissé tel quel
[ ] branche noavgref pour cov/cosp — abandonnée (comparabilité Arthur osef)
[ ] Hann vs Hamming — TRANCHÉ : Hann (meilleur roll-off bandes larges)
[ ] Autoreject pré-ICA — écarté (mal adapté à l'EEG de sommeil continu,
    le HP 1 Hz sur la copie de fit est le garde-fou suffisant)
[ ] psd_peak_* (feature peaks FOOOF) — parked jusqu'à décision knee/fixed (#7)
[ ] LZC — exclu définitivement (trop corrélé à l'exposant aperiodic)

================================================================================
NOTES RAPIDES
================================================================================

- s24 : re-preprocessé avec 128G (11h09 de données, OOM à 64G) → OK
- s26/s28 : 0 rejet EOG avec z-score 2.5 malgré artefacts visuels → à corriger
  via l'analyse des seuils (point 1)
- ICLabel : définitivement inadapté comme méthode principale sur ce sommeil
  (classe les artefacts en "other", même à seuil 0.3). Gardé comme comparaison.
- misc1/2/3 : toujours non identifiés, jamais utilisés, ignorés en aval
- Picard : remplace FastICA sur les deux branches ICA (reco doc MNE pour EEG réel)
- Permutation testing : niveau sujet (Combrisson & Jerbi 2015), pas epoch-level
- Seeding reproductible : hashlib.md5, pas Python hash() (non-déterministe)
